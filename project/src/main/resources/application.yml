spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/project
    username: sa
    password:
    driver-class-name: org.h2.Driver

  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    hibernate:
      #ddl-auto: create
    properties:
      hibernate:
       # show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

